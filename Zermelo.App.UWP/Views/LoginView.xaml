<Page
    x:Class="Zermelo.App.UWP.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Zermelo.App.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="12" MaxWidth="400">
            <TextBox Header="School" Text="{x:Bind ViewModel.School, Mode=TwoWay}" />
            <TextBox Header="Token" Text="{x:Bind ViewModel.Token, Mode=TwoWay}" />
            <Button Margin="12" HorizontalAlignment="Center" Content="Log in" Command="{x:Bind ViewModel.LogIn}" />
        </StackPanel>
    </Grid>
</Page>
